# YML file for setting up Python environment
name: lidar
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.11
  - pip
  - python-dotenv
  - pytest
  - lxml
  - pandas<2.2
  - geopandas
  - rasterio
  - geojson
  - python-pdal
  - pdal
  - gdal
  - pygeos
  - shapely
  - osmnx
  - psycopg2
  - sqlalchemy<2.0  # v2.0 does not supported by pandas 2.1.x packages yet.
  - geoalchemy2
  - scrapy
  - dask
  - bokeh!=3.0.*,>=2.4.2  # for Dask dashboard
  - leafmap  # for jupyter notebook, delete it if no need
  - pip:
    - geofabrics==1.0.0
prefix:
